alertmanager:
  config:
    global:
      resolve_timeout: 5m
    route:
      receivers: 'discord'
    receivers:
      - name: 'discord'
        webhook_configs:
          - url: "http://am-discord.monitoring.svc.cluster.local:9094/"
