apiVersion: v1
kind: Service
metadata:
  name: trilium-mcp
  namespace: trilium-mcp
  labels: { app: trilium-mcp }
spec:
  selector: { app: trilium-mcp }
  ports:
    - name: http
      port: 8080
      targetPort: http
